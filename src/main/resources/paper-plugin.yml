name: GUIManager
version: @version@
main: com.mcdragonmasters.guiManager.GUIManager
api-version: '1.21'
load: STARTUP
authors: [MCdragonmasters]
description: E
dependencies:
  server:
    Vault:
      load: BEFORE
      required: true
      join-classpath: true